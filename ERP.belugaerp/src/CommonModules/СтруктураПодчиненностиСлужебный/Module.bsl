&Вместо("ИндексТиповСвязанныхОбъектов")
Функция бг_ИндексТиповСвязанныхОбъектов()
	
	// Выполняем типовой метод ИндексТиповСвязанныхОбъектов(), который получает значения из типов стандартного
	// критерия отбора СвязанныеДокументы.
	Индекс = ПродолжитьВызов();

	// Выполняем аналогичные типовому методу ИндексТиповСвязанныхОбъектов() действия, но по критерию отбора из
	// расширения - бг_СвязанныеДокументы.
	МетаданныеСвязанныхОбъектов = Метаданные.КритерииОтбора.бг_СвязанныеДокументы;
	ТипыСвязанныхОбъектов = МетаданныеСвязанныхОбъектов.Тип.Типы();
	ТипПараметраКоманды = Метаданные.ОбщиеКоманды.СвязанныеДокументы.ТипПараметраКоманды;
	
	Для Каждого ТипСвязанногоОбъекта Из ТипыСвязанныхОбъектов Цикл 
		
		Если Не ТипПараметраКоманды.СодержитТип(ТипСвязанногоОбъекта) Тогда 
			Индекс.Вставить(ТипСвязанногоОбъекта, Истина);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Индекс;
	
КонецФункции
