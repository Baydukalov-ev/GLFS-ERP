#Область ОбработчикиСобытийФормы

&НаСервере
Процедура бг_ПередЗаписьюНаСервереПеред(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	Если Не Пользователи.РолиДоступны("ПолныеПрава") Тогда
		ТекстОшибки = НСтр("ru = 'Статьи ДДС интегрируются из базы НСИ.
                            |Добавление/изменение доступно только пользователям с полными правами'");
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки, , , , Отказ);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
