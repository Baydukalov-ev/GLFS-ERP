#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоНовый() Тогда
		Добавлен = ТекущаяДатаСеанса();
	КонецЕсли;
	
	бг_ОбщегоНазначенияСервер.ПередЗаписьюСправочникаНСИ(Отказ, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти
